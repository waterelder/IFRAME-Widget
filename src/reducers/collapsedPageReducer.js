/*
 * @flow
 *
 * */

import type {CollapsedPageState, Action} from '../types/types';

const initialState:CollapsedPageState = {

};

export default function collapsedPage(state: CollapsedPageState = initialState, action: Action<any>):CollapsedPageState {
    return state;
};